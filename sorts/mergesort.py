'''
split func
accept input
if len <= 1
	return input
else 
	mid = len unsorted / 2
	left = 0
	right = mid

sort func
while len left > 0 and len right > 0
	if len left < 1
		append right(all)
	else if len right < 1
		append left(all)
	else
		if left < right
			append left
		else append right
'''
